<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.haozi.ttqk.mapper.TiktokUserMapper">

    <select id="checkIsUserExist" resultType="java.lang.Integer">
        SELECT count(*) from tt_tiktok_user where is_delete=0 and (user_id = #{userId} or name=#{name} or tiktok_id=#{tiktokId})
    </select>



</mapper>